# build
/bin/
/dist/
/pool-proxy
*.exe
*.test
*.out

# data & local state (never commit tokens/db)
/data/
*.db
*.db-wal
*.db-shm
*.ndjson
.env
*.local.yaml
settings.json

# profiles / benches
*.pprof
*.prof
cpu.out
mem.out

# frontend
frontend/node_modules/
frontend/dist/
# embed copy is committed/generated by make frontend-build for local go build;
# Docker builds its own copy. Keep local adminui/dist out of git noise if regenerated.
# (CI/Docker generate it; developers run `make frontend-build` before `go build`.)

# IDE / OS / agent
.idea/
.vscode/
.DS_Store
.claude/
__pycache__/
